---
title: greentic-setup CLI reference
description: Auto-generated --help reference for the greentic-setup CLI.
sidebar:
  label: greentic-setup
  order: 10
---

{/* AUTO-GENERATED by scripts/sync-cli-help.mjs — do not edit by hand. */}
{/* Curated narrative for this CLI lives under src/content/docs/cli/. */}

**Binary version:** `greentic-setup 1.1.25`

This page mirrors the CLI's `--help` output so it stays accurate against the installed binary. If you're looking for narrative and examples, see the curated pages linked in the sidebar.

## Root help

```text
Greentic bundle setup CLI

Usage: greentic-setup [OPTIONS] [BUNDLE] [COMMAND]

Commands:
  doctor      Diagnose bundle setup inputs and generated setup outputs
  env-deploy  Deploy a bundle into an environment via the env-apply engine
  provider    Manage messaging providers in an environment
  bundle      Bundle lifecycle management (advanced)
  help        Print this message or the help of the given subcommand(s)

Arguments:
  [BUNDLE]  Bundle path (.gtbundle file or directory)

Options:
      --dry-run              Dry run - show wizard but don't execute
      --emit-answers <FILE>  Emit answers template to file (combine with --dry-run to only generate)
  -a, --answers <FILE>       Apply answers from file
      --key <KEY>            Encryption/decryption key for answer documents that include secrets
  -t, --tenant <TENANT>      Tenant identifier [default: demo]
      --team <TEAM>          Team identifier
  -e, --env <ENV>            Environment (defaults to `local`; legacy `dev` remapped via the A4b compat alias with a once-per-process warning until removal) [default: local]
      --locale <LOCALE>      UI locale (BCP-47 tag, e.g., en, ja, id)
      --advanced             Advanced mode — show all questions including optional ones
      --ui                   Launch web-based setup UI in browser (enabled by default). Use --no-ui to disable the UI; stdin prompts may still be used
      --no-ui                Disable web UI; stdin prompts may still be used
      --non-interactive      Strict non-interactive mode: no prompts, fail if answers incomplete
      --new-cache            Ignore any cached state: clear the persisted env secrets store and the bundle's cached setup-state / extraction dirs before running, so setup rebuilds from scratch instead of reusing prior values
  -h, --help                 Print help
  -V, --version              Print version

EXAMPLES:
  Interactive wizard:
    greentic-setup ./my-bundle

  Preview without executing:
    greentic-setup --dry-run ./my-bundle

  Generate answers template:
    greentic-setup --dry-run --emit-answers answers.json ./my-bundle

  Apply answers file:
    greentic-setup --answers answers.json ./my-bundle.gtbundle

  Deploy a bundle into an environment:
    greentic-setup env-deploy ./my-bundle.gtbundle
    greentic-setup env-deploy ./my-bundle.gtbundle --env staging
    greentic-setup env-deploy --dry-run ./my-bundle.gtbundle

  Add a messaging provider to an environment:
    greentic-setup provider add telegram
    greentic-setup provider add slack --env staging
    greentic-setup provider add telegram --answers answers.json --non-interactive
    greentic-setup provider list
    greentic-setup provider remove <endpoint-id>

  Advanced (bundle subcommands):
    greentic-setup bundle init ./my-bundle
    greentic-setup bundle add pack.gtpack --bundle ./my-bundle
    greentic-setup bundle status --bundle ./my-bundle
```

## Subcommands

### `greentic-setup doctor`

```text
Diagnose bundle setup inputs and generated setup outputs

Usage: greentic-setup doctor [OPTIONS] [BUNDLE] [COMMAND]

Commands:
  provider  Validate a provider pack's setup contract
  help      Print this message or the help of the given subcommand(s)

Arguments:
  [BUNDLE]  Bundle path (.gtbundle file or directory)

Options:
      --dry-run              Dry run - show wizard but don't execute
      --emit-answers <FILE>  Emit answers template to file (combine with --dry-run to only generate)
      --json                 Emit stable machine-readable JSON
  -a, --answers <FILE>       Apply answers from file
      --strict               Treat warnings as command failures
      --fix-hints            Include fix hints in human-readable output
      --key <KEY>            Encryption/decryption key for answer documents that include secrets
      --show-info            Show informational diagnostics in human-readable output
  -t, --tenant <TENANT>      Tenant identifier [default: demo]
      --stage <STAGE>        Limit checks to one stage [possible values: setup, cache, locks, answers, runtime, routes]
      --team <TEAM>          Team identifier
  -e, --env <ENV>            Environment (defaults to `local`; legacy `dev` remapped via the A4b compat alias with a once-per-process warning until removal) [default: local]
      --locale <LOCALE>      UI locale (BCP-47 tag, e.g., en, ja, id)
      --advanced             Advanced mode — show all questions including optional ones
      --ui                   Launch web-based setup UI in browser (enabled by default). Use --no-ui to disable the UI; stdin prompts may still be used
      --no-ui                Disable web UI; stdin prompts may still be used
      --non-interactive      Strict non-interactive mode: no prompts, fail if answers incomplete
      --new-cache            Ignore any cached state: clear the persisted env secrets store and the bundle's cached setup-state / extraction dirs before running, so setup rebuilds from scratch instead of reusing prior values
  -h, --help                 Print help
```

### `greentic-setup env-deploy`

```text
Deploy a bundle into an environment via the env-apply engine

Usage: greentic-setup env-deploy [OPTIONS] <BUNDLE>

Arguments:
  <BUNDLE>  Bundle path (.gtbundle file or bundle directory)

Options:
      --dry-run              Dry run - show wizard but don't execute
      --emit-answers <FILE>  Emit answers template to file (combine with --dry-run to only generate)
  -a, --answers <FILE>       Apply answers from file
      --key <KEY>            Encryption/decryption key for answer documents that include secrets
  -t, --tenant <TENANT>      Tenant identifier [default: demo]
      --team <TEAM>          Team identifier
  -e, --env <ENV>            Environment (defaults to `local`; legacy `dev` remapped via the A4b compat alias with a once-per-process warning until removal) [default: local]
      --locale <LOCALE>      UI locale (BCP-47 tag, e.g., en, ja, id)
      --advanced             Advanced mode — show all questions including optional ones
      --ui                   Launch web-based setup UI in browser (enabled by default). Use --no-ui to disable the UI; stdin prompts may still be used
      --no-ui                Disable web UI; stdin prompts may still be used
      --non-interactive      Strict non-interactive mode: no prompts, fail if answers incomplete
      --new-cache            Ignore any cached state: clear the persisted env secrets store and the bundle's cached setup-state / extraction dirs before running, so setup rebuilds from scratch instead of reusing prior values
  -h, --help                 Print help
```

### `greentic-setup provider`

```text
Manage messaging providers in an environment

Usage: greentic-setup provider [OPTIONS] <COMMAND>

Commands:
  add     Add a messaging provider to an environment
  list    List messaging providers in an environment
  remove  Remove a messaging provider from an environment
  help    Print this message or the help of the given subcommand(s)

Options:
      --dry-run              Dry run - show wizard but don't execute
      --emit-answers <FILE>  Emit answers template to file (combine with --dry-run to only generate)
  -a, --answers <FILE>       Apply answers from file
      --key <KEY>            Encryption/decryption key for answer documents that include secrets
  -t, --tenant <TENANT>      Tenant identifier [default: demo]
      --team <TEAM>          Team identifier
  -e, --env <ENV>            Environment (defaults to `local`; legacy `dev` remapped via the A4b compat alias with a once-per-process warning until removal) [default: local]
      --locale <LOCALE>      UI locale (BCP-47 tag, e.g., en, ja, id)
      --advanced             Advanced mode — show all questions including optional ones
      --ui                   Launch web-based setup UI in browser (enabled by default). Use --no-ui to disable the UI; stdin prompts may still be used
      --no-ui                Disable web UI; stdin prompts may still be used
      --non-interactive      Strict non-interactive mode: no prompts, fail if answers incomplete
      --new-cache            Ignore any cached state: clear the persisted env secrets store and the bundle's cached setup-state / extraction dirs before running, so setup rebuilds from scratch instead of reusing prior values
  -h, --help                 Print help
```

### `greentic-setup bundle`

```text
Bundle lifecycle management (advanced)

Usage: greentic-setup bundle [OPTIONS] <COMMAND>

Commands:
  init           Initialize a new bundle directory
  add            Add a pack to a bundle
  setup          Run setup flow for provider(s) in a bundle
  update         Update a provider's configuration in a bundle
  setup-status   Show persisted generic provider setup status
  setup-next     Inspect and record the next generic provider setup step
  setup-retry    Clear retry-blocking state for a generic provider setup step
  setup-reset    Reset persisted generic provider setup state
  setup-migrate  Migrate legacy provider setup state into the generic setup state layout
  remove         Remove a provider from a bundle
  build          Build a portable bundle (copy + resolve)
  list           List packs or flows in a bundle
  status         Show bundle status
  help           Print this message or the help of the given subcommand(s)

Options:
      --dry-run              Dry run - show wizard but don't execute
      --emit-answers <FILE>  Emit answers template to file (combine with --dry-run to only generate)
  -a, --answers <FILE>       Apply answers from file
      --key <KEY>            Encryption/decryption key for answer documents that include secrets
  -t, --tenant <TENANT>      Tenant identifier [default: demo]
      --team <TEAM>          Team identifier
  -e, --env <ENV>            Environment (defaults to `local`; legacy `dev` remapped via the A4b compat alias with a once-per-process warning until removal) [default: local]
      --locale <LOCALE>      UI locale (BCP-47 tag, e.g., en, ja, id)
      --advanced             Advanced mode — show all questions including optional ones
      --ui                   Launch web-based setup UI in browser (enabled by default). Use --no-ui to disable the UI; stdin prompts may still be used
      --no-ui                Disable web UI; stdin prompts may still be used
      --non-interactive      Strict non-interactive mode: no prompts, fail if answers incomplete
      --new-cache            Ignore any cached state: clear the persisted env secrets store and the bundle's cached setup-state / extraction dirs before running, so setup rebuilds from scratch instead of reusing prior values
  -h, --help                 Print help
```
