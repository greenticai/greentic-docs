---
title: greentic-start CLI reference
description: Auto-generated --help reference for the greentic-start CLI.
sidebar:
  label: greentic-start
  order: 10
---

{/* AUTO-GENERATED by scripts/sync-cli-help.mjs — do not edit by hand. */}
{/* Curated narrative for this CLI lives under src/content/docs/cli/. */}

**Binary version:** `greentic-start 1.1.28`

This page mirrors the CLI's `--help` output so it stays accurate against the installed binary. If you're looking for narrative and examples, see the curated pages linked in the sidebar.

## Root help

```text
Usage: greentic-start [OPTIONS] <COMMAND>

Commands:
  start    
  up       
  stop     
  restart  
  warmup   
  doctor   
  help     Print this message or the help of the given subcommand(s)

Options:
      --locale <LOCALE>  
  -h, --help             Print help
  -V, --version          Print version
```

## Subcommands

### `greentic-start start`

```text
Usage: greentic-start start [OPTIONS]

Options:
      --bundle <BUNDLE>
          
      --locale <LOCALE>
          
      --env <ENV>
          Environment id whose persisted state the bundle-less boot serves. Wins over `$GREENTIC_ENV`; defaults to `local`. Ignored (with a warning) on the legacy `--bundle` / `--config` path, which has no environment concept
      --tenant <TENANT>
          
      --team <TEAM>
          
      --nats <NATS>
          [default: off] [possible values: off, on, external]
      --nats-url <NATS_URL>
          
      --config <CONFIG>
          
      --cloudflared <CLOUDFLARED>
          [default: off] [possible values: on, off]
      --cloudflared-binary <CLOUDFLARED_BINARY>
          
      --ngrok <NGROK>
          [default: off] [possible values: on, off]
      --ngrok-binary <NGROK_BINARY>
          
      --runner-binary <RUNNER_BINARY>
          
      --restart <RESTART>
          [possible values: all, cloudflared, ngrok, nats, gateway, egress, subscriptions]
      --log-dir <DIR>
          
      --verbose
          
      --quiet
          
      --no-browser
          Do not open the first web UI URL in the default browser
      --no-updates
          Do not subscribe this runtime to its environment's update channel
      --no-auto-restart
          Do not auto-restart after a binary self-update (stage only, like P7d)
      --admin
          Enable mTLS admin API endpoint
      --admin-port <ADMIN_PORT>
          Port for the admin API endpoint [default: 8443]
      --admin-certs-dir <DIR>
          Directory containing admin TLS certs (server.crt, server.key, ca.crt)
      --admin-allowed-clients <ADMIN_ALLOWED_CLIENTS>
          Comma-separated list of allowed client CNs (empty = allow all valid certs)
  -h, --help
          Print help
```

### `greentic-start up`

```text
Usage: greentic-start up [OPTIONS]

Options:
      --bundle <BUNDLE>
          
      --locale <LOCALE>
          
      --env <ENV>
          Environment id whose persisted state the bundle-less boot serves. Wins over `$GREENTIC_ENV`; defaults to `local`. Ignored (with a warning) on the legacy `--bundle` / `--config` path, which has no environment concept
      --tenant <TENANT>
          
      --team <TEAM>
          
      --nats <NATS>
          [default: off] [possible values: off, on, external]
      --nats-url <NATS_URL>
          
      --config <CONFIG>
          
      --cloudflared <CLOUDFLARED>
          [default: off] [possible values: on, off]
      --cloudflared-binary <CLOUDFLARED_BINARY>
          
      --ngrok <NGROK>
          [default: off] [possible values: on, off]
      --ngrok-binary <NGROK_BINARY>
          
      --runner-binary <RUNNER_BINARY>
          
      --restart <RESTART>
          [possible values: all, cloudflared, ngrok, nats, gateway, egress, subscriptions]
      --log-dir <DIR>
          
      --verbose
          
      --quiet
          
      --no-browser
          Do not open the first web UI URL in the default browser
      --no-updates
          Do not subscribe this runtime to its environment's update channel
      --no-auto-restart
          Do not auto-restart after a binary self-update (stage only, like P7d)
      --admin
          Enable mTLS admin API endpoint
      --admin-port <ADMIN_PORT>
          Port for the admin API endpoint [default: 8443]
      --admin-certs-dir <DIR>
          Directory containing admin TLS certs (server.crt, server.key, ca.crt)
      --admin-allowed-clients <ADMIN_ALLOWED_CLIENTS>
          Comma-separated list of allowed client CNs (empty = allow all valid certs)
  -h, --help
          Print help
```

### `greentic-start stop`

```text
Usage: greentic-start stop [OPTIONS]

Options:
      --bundle <BUNDLE>        
      --locale <LOCALE>        
      --env <ENV>              Environment id whose serving runtime should be stopped. Wins over `$GREENTIC_ENV`; defaults to `local`. Ignored on the legacy `--bundle` / `--state-dir` path
      --state-dir <STATE_DIR>  
      --tenant <TENANT>        [default: demo]
      --team <TEAM>            [default: default]
  -h, --help                   Print help
```

### `greentic-start restart`

```text
Usage: greentic-start restart [OPTIONS]

Options:
      --bundle <BUNDLE>
          
      --locale <LOCALE>
          
      --env <ENV>
          Environment id whose persisted state the bundle-less boot serves. Wins over `$GREENTIC_ENV`; defaults to `local`. Ignored (with a warning) on the legacy `--bundle` / `--config` path, which has no environment concept
      --tenant <TENANT>
          
      --team <TEAM>
          
      --nats <NATS>
          [default: off] [possible values: off, on, external]
      --nats-url <NATS_URL>
          
      --config <CONFIG>
          
      --cloudflared <CLOUDFLARED>
          [default: off] [possible values: on, off]
      --cloudflared-binary <CLOUDFLARED_BINARY>
          
      --ngrok <NGROK>
          [default: off] [possible values: on, off]
      --ngrok-binary <NGROK_BINARY>
          
      --runner-binary <RUNNER_BINARY>
          
      --restart <RESTART>
          [possible values: all, cloudflared, ngrok, nats, gateway, egress, subscriptions]
      --log-dir <DIR>
          
      --verbose
          
      --quiet
          
      --no-browser
          Do not open the first web UI URL in the default browser
      --no-updates
          Do not subscribe this runtime to its environment's update channel
      --no-auto-restart
          Do not auto-restart after a binary self-update (stage only, like P7d)
      --admin
          Enable mTLS admin API endpoint
      --admin-port <ADMIN_PORT>
          Port for the admin API endpoint [default: 8443]
      --admin-certs-dir <DIR>
          Directory containing admin TLS certs (server.crt, server.key, ca.crt)
      --admin-allowed-clients <ADMIN_ALLOWED_CLIENTS>
          Comma-separated list of allowed client CNs (empty = allow all valid certs)
  -h, --help
          Print help
```

### `greentic-start warmup`

```text
Usage: greentic-start warmup [OPTIONS] --bundle <BUNDLE>

Options:
      --bundle <BUNDLE>  Path to a setup-resolved bundle directory whose components should be precompiled
      --locale <LOCALE>  
      --cache-dir <DIR>  Cache root directory. Defaults to `${GREENTIC_CACHE_DIR}` or `.greentic/cache/components`
      --strict           Fail on the first compile error instead of counting it as skipped
  -h, --help             Print help
```

### `greentic-start doctor`

```text
Usage: greentic-start doctor [OPTIONS] [BUNDLE]

Arguments:
  [BUNDLE]  Bundle reference or extracted bundle directory to inspect. When omitted, doctor checks the environment-store readiness of `--env` (or the resolved default env) instead

Options:
      --env <ENV>        Environment id whose store-backed runtime readiness should be checked (trust root, messaging-endpoint linkage, secret-ref resolvability). Defaults to `$GREENTIC_ENV` / `local` when no bundle is given
      --locale <LOCALE>  
      --json             Emit stable machine-readable JSON
      --strict           Promote drift/tag/cache and env-readiness warnings to errors
      --fix-hints        Include longer remediation hints in human output
      --show-info        Include informational checks in output
      --stage <STAGE>    Restrict checks to one diagnostic stage [default: all] [possible values: all, setup, cache, locks, answers, runtime, routes, provider, secrets]
  -h, --help             Print help
```
